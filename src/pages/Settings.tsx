import { Bell, Check, Palette, Save, ShieldAlert, SlidersHorizontal } from 'lucide-react';
import { useState } from 'react';
import type { ReactNode } from 'react';
import { DemoNotice, PageHeader } from '../components/platform/PageHeader';
import { Panel } from '../components/platform/MetricCard';

export function Settings() {
  const [notifications, setNotifications] = useState(true);
  const [criticalOnly, setCriticalOnly] = useState(false);
  const [compactMode, setCompactMode] = useState(false);
  const [saved, setSaved] = useState(false);

  function markSaved(): void {
    setSaved(true);
    window.setTimeout(() => setSaved(false), 1600);
  }

  return <div className="mx-auto max-w-5xl"><PageHeader eyebrow="Workspace configuration" title="Settings" description="Review the product configuration surface. Only local preferences are interactive in this phase." actions={<button type="button" onClick={markSaved} className="inline-flex items-center gap-2 rounded-lg bg-accent px-3.5 py-2 text-xs font-semibold text-white shadow-card hover:bg-accent/90 focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2">{saved ? <Check className="h-4 w-4" aria-hidden="true" /> : <Save className="h-4 w-4" aria-hidden="true" />}{saved ? 'Local changes noted' : 'Save preferences'}</button>} /><DemoNotice>Settings are local to this browser session · no persistence or backend configuration is active</DemoNotice><div className="space-y-4"><Panel title="General" eyebrow="Workspace identity"><div className="grid grid-cols-1 gap-4 sm:grid-cols-2"><label className="text-xs font-medium text-ink-muted">Workspace name<input defaultValue="ModeraAI Demo Workspace" className="mt-1.5 w-full rounded-lg border border-line bg-panel px-3 py-2 text-sm text-ink outline-none focus:border-accent focus:ring-2 focus:ring-accent/20" /></label><label className="text-xs font-medium text-ink-muted">Default review priority<select defaultValue="risk" className="mt-1.5 w-full rounded-lg border border-line bg-panel px-3 py-2 text-sm text-ink outline-none focus:border-accent focus:ring-2 focus:ring-accent/20"><option value="risk">Highest risk first</option><option value="age">Oldest first</option></select></label></div></Panel><Panel title="Moderation policies" eyebrow="Read-only foundation"><div className="flex items-start gap-3 rounded-lg border border-line bg-panel p-3"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-moderate" aria-hidden="true" /><div><p className="text-sm font-medium text-ink">Default demo policy</p><p className="mt-1 text-xs leading-relaxed text-ink-muted">The current dashboard uses the deterministic demo thresholds from the existing simulation. Policy editing will be connected in a later phase.</p></div></div></Panel><Panel title="Notifications" eyebrow="Local preference"><div className="space-y-3"><Toggle label="Moderation activity" description="Show local notifications for demo queue changes." checked={notifications} onChange={setNotifications} icon={<Bell className="h-4 w-4" aria-hidden="true" />} /><Toggle label="Critical events only" description="Limit the local demo signal to critical items." checked={criticalOnly} onChange={setCriticalOnly} icon={<ShieldAlert className="h-4 w-4" aria-hidden="true" />} /></div></Panel><Panel title="Appearance and preferences" eyebrow="Local preference"><Toggle label="Compact data density" description="Reduce spacing in future data-heavy surfaces. Existing moderation layout is unchanged." checked={compactMode} onChange={setCompactMode} icon={<SlidersHorizontal className="h-4 w-4" aria-hidden="true" />} /><div className="mt-4 flex items-start gap-3 border-t border-line pt-4"><Palette className="mt-0.5 h-4 w-4 shrink-0 text-ink-faint" aria-hidden="true" /><div><p className="text-sm font-medium text-ink">Theme</p><p className="mt-1 text-xs text-ink-muted">Light theme is the current product baseline. Theme persistence is not implemented.</p></div></div></Panel></div></div>;
}

function Toggle({ label, description, checked, onChange, icon }: { label: string; description: string; checked: boolean; onChange: (value: boolean) => void; icon: ReactNode }) {
  return <div className="flex items-center justify-between gap-4"><div className="flex items-start gap-3"><span className="mt-0.5 text-ink-faint">{icon}</span><div><p className="text-sm font-medium text-ink">{label}</p><p className="mt-1 text-xs text-ink-muted">{description}</p></div></div><button type="button" role="switch" aria-checked={checked} aria-label={label} onClick={() => onChange(!checked)} className={['relative h-6 w-11 shrink-0 rounded-full transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2', checked ? 'bg-accent' : 'bg-line'].join(' ')}><span className={['absolute top-0.5 h-5 w-5 rounded-full bg-white shadow-card transition-transform', checked ? 'translate-x-[22px]' : 'translate-x-0.5'].join(' ')} /></button></div>;
}
