import { Boxes, CircleDot, Info } from 'lucide-react';
import { DemoNotice, PageHeader } from '../components/platform/PageHeader';
import { demoModels } from '../data/demo/models';
import { StatusPill } from '../components/platform/StatusPill';

export function Models() {
  return <div className="mx-auto max-w-[1600px]"><PageHeader eyebrow="Model operations" title="Model registry" description="A catalog of modality models and evaluation placeholders for the future inference platform." /><DemoNotice>Demo registry records · deployment actions are intentionally unavailable</DemoNotice><div className="overflow-hidden rounded-xl border border-line bg-surface shadow-card"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-line px-4 py-3.5"><div className="flex items-center gap-2"><Boxes className="h-4 w-4 text-accent" aria-hidden="true" /><p className="text-sm font-semibold text-ink">Registered models</p></div><span className="text-[11px] text-ink-faint">{demoModels.length} demo records</span></div><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left"><thead className="bg-panel"><tr className="border-b border-line text-[10px] uppercase tracking-wider text-ink-faint"><th className="px-4 py-3 font-semibold">Model</th><th className="px-3 py-3 font-semibold">Modality</th><th className="px-3 py-3 font-semibold">Status</th><th className="px-3 py-3 font-semibold">Deployment</th><th className="px-3 py-3 font-semibold">Precision</th><th className="px-3 py-3 font-semibold">Recall</th><th className="px-3 py-3 font-semibold">F1</th><th className="px-3 py-3 font-semibold">ROC-AUC</th><th className="px-3 py-3 font-semibold">Created</th></tr></thead><tbody>{demoModels.map((model) => <tr key={model.id} className="border-b border-line-soft last:border-0 hover:bg-panel"><td className="px-4 py-3"><p className="text-sm font-semibold text-ink">{model.name}</p><p className="mt-0.5 font-mono text-[11px] text-ink-faint">{model.version}</p></td><td className="px-3 py-3"><span className="capitalize text-xs text-ink-muted">{model.modality}</span></td><td className="px-3 py-3"><StatusPill status={model.status} /></td><td className="px-3 py-3"><span className="inline-flex items-center gap-1.5 text-xs text-ink-muted"><CircleDot className="h-3.5 w-3.5 text-ink-faint" aria-hidden="true" />{model.deployment}</span></td><td className="px-3 py-3 font-mono text-xs font-semibold text-ink">{model.precision}</td><td className="px-3 py-3 font-mono text-xs font-semibold text-ink">{model.recall}</td><td className="px-3 py-3 font-mono text-xs font-semibold text-ink">{model.f1}</td><td className="px-3 py-3 font-mono text-xs font-semibold text-ink">{model.rocAuc}</td><td className="px-3 py-3 text-xs text-ink-faint">{model.createdAt}</td></tr>)}</tbody></table></div></div><p className="mt-4 flex items-center gap-2 text-[11px] text-ink-faint"><Info className="h-3.5 w-3.5" aria-hidden="true" />Registry actions will remain read-only until a real model service is introduced.</p></div>;
}
